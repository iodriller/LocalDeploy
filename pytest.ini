[pytest]
# Only collect tests from the tests/ directory. test_models.py at the repo root
# is the benchmark runner (script with test_* helper names), not pytest tests.
testpaths = tests
python_files = test_*.py
